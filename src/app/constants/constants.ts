export const DEFAULT_AUSTIN_COORDINATES = [30.2672, -97.7431];

export enum Role {
	Admin = "Admin",
	Default = "Default",
}

export enum ChartTimeLine {
	MTD = "Month to date",
	YTD = "Year to Date",
	LTD = "Life to Date"
}